Link to the question: https://www.hackerrank.com/challenges/breaking-best-and-worst-records/problem?h_r=profile

My Solution:

import java.util.*;
public class Main{
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int size=sc.nextInt();
        int[] arr=new int[size];
        for(int i=0;i<arr.length;i++){
            arr[i]=sc.nextInt();
        }
        breakingRecords(arr,size);
    }

    public static void breakingRecords(int[] arr,int size){
       int largest=arr[0];
       int smallest=arr[0];
       int count1=0;
       int count2=0;
      for(int i=1;i<arr.length;i++){
          if(arr[i]>largest){
            largest=arr[i];
            count1++;
          }
          else
          continue;
      }
         for(int i=1;i<arr.length;i++){
          if(arr[i]<smallest){
            smallest=arr[i];
            count2++;
          }
          else
          continue;
      }
            System.out.println(count1 + " " + count2);
    }
      
}
